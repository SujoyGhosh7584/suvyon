services:
  - type: web
    name: suvy-on-backend
    runtime: python
    pythonVersion: 3.12.11

    buildCommand: pip install -r requirements.txt

    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT

    envVars:
      - key: ENVIRONMENT
        value: production
# UI preview

These browser screenshots show the Suvyon redesign. Workspace content uses local test fixtures, not real user data.

- `landing-desktop.png`: new public landing page.
- `login-desktop.png`: shared authentication design.
- `chat-mobile.png`: conversation layout with sample messages.

Brand source files are in `../brand` and runtime SVG copies in `frontend/public/brand`.
